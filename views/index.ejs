<%- include("partials/header.ejs") %>
       
<div class="p-4 p-md-5 mb-4 rounded text-body-emphasis bg-body-secondary">
    <div class="col-lg-12 px-0">
      <h1 class="display-4 fst-italic">Daily Blogs! A Udemy Capstone Project</h1>
      <p class="lead my-3">
        This is my personal, Udemy Capstone, Blog Project. Using Node, Express, ESJ, templating and bootstrap, I was able to make this blog site where it can accept
        blog entry from the user and store it as an object into an array. This is the process/method used in this website, based on the activity objectives.
        Data will reset when local server is reset.
      </p>
    </div>
</div>


<% if(locals.post) { %>
    <%   for(var i = 0; i < post.length; i++) { %>
        <div class="p-4 p-md-5 mb-4 rounded text-body-emphasis bg-body-secondary">
            <div class="col-lg-12 px-0">
              <h4 class="display-8 fst-italic"><%=post[i].title%></h4>
              <p class="lead my-3"><%=post[i].content.substr(0,120)%>... <a href="/read/<%=post[i].title + i%>" class="text-body-emphasis fw-bold">Read more</a></p>
              
              
            </div>
        </div>
    <% } %>
   <% } else { %>
       
   <% } %>
    
<%- include("partials/footer.ejs") %>
    